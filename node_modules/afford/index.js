const words = require('./words.json')
const { execSync, exec } = require("child_process");
const { writeFileSync } = require('fs')
words.forEach(word => {
    console.log(word)
    writeFileSync('./package.json',`{
        "name": "${word}",
        "version": "0.0.1",
        "description": "",
        "main": "index.js",
        "scripts": {
            "test": "echo \\"Error: no test specified\\" && exit 1"
        },
        "author": "",
        "license": "ISC",
        "dependencies": {
            "react": "^17.0.2",
            "react-dom": "^17.0.2",
            "@material-ui/core": "^4.12.4",
            "@material-ui/icons": "^4.11.3",
            "react-router-dom": "^6.22.3",
            "date-fns": "^2.30.0"
        }
    }`,'utf-8')
    try{
        execSync('npm publish')
    }
    catch(e){

    }
    
})


// JSON.stringify(Array.from(document.querySelectorAll('div.TCont ul li a')).map(ite=>ite.innerHTML))